#include "main.h"

/**
 * _islower - check for lower case characters
 * @c: The number to be checked
 *
 * Return: Always 1 if a is lower and 0 otherwise
 */
int _islower(int c)
{
    return (c >= 97 && c <= 122);
}
